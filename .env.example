# Bootstrap and deployment configuration only. Runtime settings belong in
# config/platform.settings.json, copied from config/platform.settings.example.json.
# Never put secrets in either file.
PLATFORM_SETTINGS_PATH=config/platform.settings.json
PLATFORM_DATA_DIR=data
PLATFORM_LOG_DIR=data/logs
PLATFORM_MEMORY_DB=data/memory/platform_memory.sqlite3
